export class Toy {
    price: number ;
    name: string;
    age: string;
    gender: string;

    constructor(
        price: number,
        name: string,
        age: string,
        gender: string
        ) {this.price = price, this.name = name, this.age = age, this.gender = gender}

}
